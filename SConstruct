env = Environment()
Help("""
   Type: 'scons main' to build the main program,
         'scons test' to build the test program.
""")

main = env.Program(target='main' , source = ['main.cpp','datum/datum.cpp','datum/datumformat.cpp','datum/datumformaten.cpp','datum/datumformatde.cpp'])
Default(main)
